"""Stock-Research-Agent package.

All source files live directly in this directory (``agents/stock_research_agent/``).

The legacy ``agents/stock-research-agent/`` directory (hyphenated) is retained as
a git-tracked copy of the originals but is no longer the active code path.
Consumers should import from ``agents.stock_research_agent.agent``.
"""
