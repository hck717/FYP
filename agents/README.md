# Agent Logic
This directory contains the core reasoning engines for the project.

## Structure
- **/supervisor**: The "Planner" agent (GPT-4o) that understands user intent and routes tasks.
- **/critic**: The "Auditor" agent that verifies claims against retrieved chunks.
- **/summarizer**: The "Synthesizer" agent that compiles final markdown reports.
