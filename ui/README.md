# UI / Frontend

This directory contains the Streamlit web application that serves as the user interface for The Agentic Investment Analyst.

## Running the UI

You can run the UI locally using:
```bash
cd ..
streamlit run ui/app.py
```
Or, start the full stack including the UI and ngrok tunneling via the `start_with_ngrok.sh` script in the project root.
